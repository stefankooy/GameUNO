package ss.uno.cards;

import ss.uno.Card;
import ss.uno.Color;

public class WildDrawCard extends Card {

    /**
     * Create a new card
     *
     * @param cardCode  : the code of the card
     */
    public WildDrawCard(int cardCode) {
        super(69, Color.BLACK, cardCode);
    }
}
